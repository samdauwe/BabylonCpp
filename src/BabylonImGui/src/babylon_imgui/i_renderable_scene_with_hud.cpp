#include <babylon/babylon_imgui/i_renderable_scene_with_hud.h>

namespace BABYLON
{
  IRenderableSceneWithHud::IRenderableSceneWithHud(ICanvas* iCanvas /*= nullptr*/) 
    : IRenderableScene(iCanvas)
  {
  }
}
